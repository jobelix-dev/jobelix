# Frontend / public keys (safe to expose)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=public-anon-key
NEXT_PUBLIC_HCAPTCHA_SITEKEY=your-hcaptcha-site-key

# Set to 'true' to skip captcha widget in local dev
# WARNING: Only works if Supabase dashboard also has captcha disabled!
# If prod Supabase has captcha enabled, login will fail without the token.
# NEXT_PUBLIC_SKIP_CAPTCHA=true

# Set this to 'true' during frontend-only development to use mock endpoints
NEXT_PUBLIC_USE_MOCKS=true

# Backend / server-only secrets (DO NOT commit to git)
# SUPABASE_SERVICE_ROLE_KEY=service-role-key
# RESEND_API_KEY=resend-api-key
# NEWSLETTER_UNSUBSCRIBE_SECRET=long-random-secret
