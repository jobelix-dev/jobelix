# Frontend / public keys (safe to expose)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=public-anon-key
NEXT_PUBLIC_HCAPTCHA_SITEKEY=your-hcaptcha-site-key

# Set this to 'true' during frontend-only development to use mock endpoints
NEXT_PUBLIC_USE_MOCKS=true

# Backend / server-only secrets (DO NOT commit to git)
# SUPABASE_SERVICE_ROLE_KEY=service-role-key
